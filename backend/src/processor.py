def process_data(data):
    # Example computation: sum the data if it's a list of numbers
    if isinstance(data, list):
        return sum(data)
    # Add your ML or other processing here
    return data
